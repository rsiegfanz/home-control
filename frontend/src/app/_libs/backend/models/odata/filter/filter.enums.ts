export enum EFilterTypes {
    NUMERIC = 'numeric',
    STRING = 'string',
    DATE = 'date',
    TIME = 'time',
    PACE = 'pace',
}

export enum EFilterOperator {
    EQUALS = '=',
    GREATER = '>',
    LESS = '<',
    GREATEREQUALS = '>=',
    LESSEQUALS = '<=',
    BETWEEN = 'zwischen',
    CONTAINS = 'enthält',
}
